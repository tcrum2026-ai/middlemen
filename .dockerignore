# Anything the image does not need, and one thing it must never have: a
# development database baked into a published image.
.data
.git
.github
.next
node_modules
npm-debug.log*
.env*
!.env.example
.netlify
tests
*.md
!README.md
